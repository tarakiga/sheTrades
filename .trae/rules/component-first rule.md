All components must be created before pages are assembled.

Rules:

Build the reusable component library first.

If a component does not exist, create it and add it to the library before using it.

Do not hard-code UI fragments inside pages when a reusable component can be created.

Pages must be thin composition layers only.

Component duplication is not allowed.