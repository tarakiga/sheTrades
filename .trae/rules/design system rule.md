The design system is the source of truth for all UI and product decisions.

Before building anything else, define and approve:

color palette

typography scale

spacing scale

radius and elevation tokens

iconography rules

layout rules

component tokens

No page, feature, or flow may be built before the design system exists.