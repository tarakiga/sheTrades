Hard-coded one-off UI is not allowed when a reusable component can represent the pattern.

Use:
- design tokens
- variants
- props
- composition

Avoid:
- inline styling for custom page-only UI
- duplicated markup
- repeated patterns that should become components