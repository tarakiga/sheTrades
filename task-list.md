# Project Task List

- `[x]` Fix Silent Failure Error States (Throw explicit errors instead of returning fallback zero-data)
- `[x]` Fix Login Page UX (Removed redundant headers, fixed excess vertical spacing between sidebar list items)
- `[x]` Empty States UX Refinement (Upgraded all data-holding pages with premium, onboarding-oriented empty states, including contextual headlines, matching section-accent primary CTAs, and illustrative icon visuals)
- `[x]` Copy Refinement (Replaced internal designer annotations with user-facing descriptions across all pages)
- `[x]` Fix Holographic Shimmer Bug (Applied flat neutral background to root layout containers)
- `[x]` Improve 500 Error Boundary Component (Implemented premium, clear server error page with retry CTA and collapsible diagnostics)
- `[x]` Resolve Badge Semantic Token Inconsistencies (Implemented a consistent 5-token badge color system across all pages, aligning Fallback Data with danger/red and other status labels semantic definitions)
- `[x]` Fix Staging 500 Server Crashes (Resolved container path seed loading ENOENT error, included seeds in Docker builder/runner stages, and added `ADMIN_ALLOW_MOCK_FALLBACK` override to allow staging fallback mock data in production mode)
- `[x]` Clean Up Redundant Manual Access Key Section (Removed the redundant access key input panels from /content page, settings integration tab, and previews since user login session tokens are now automatically synchronized, and deleted AdminAccessKeyPanel.tsx)
- `[x]` Implement Premium WhatsApp Webhook Sandbox/Simulator (Visual smartphone mockup, dialogue bubble transition simulator, active session diagnostics card, backend session reset capability, completely styled with responsive grids and interactive glowing micro-animations)
  - `[x]` Resolve React duplicate key collision console warning in simulator chat logs (`WhatsAppSandboxSimulator.tsx`)
- `[x]` Task 064: Enable WhatsApp Sandbox to Consume Dynamic Content (Seeded chatbot bot.* keys, fixed Express global error signature with exactly 4 parameters, corrected REST Status Code Mapping to 404 for missing documents, allowed camelCase keys in Zod schema validations, added 'Whatsapp Sandbox' header title, implemented onboarding greeting detection to prevent generic greetings from being processed as the user's name, pre-populated the simulator input with a default greeting 'Hello SheTrades', and added a descriptive welcome system message).
- `[x]` Task 065: Implement Content Category Classification & Interactive Filtering (Intelligent content metadata classification into Lessons & Modules, Chatbot Prompts, Language Settings, and Admin UI Copy; custom purple and teal badge colors added to the design system; a responsive grid layout combining search and category select inputs; and full resolution of TypeScript array key widening and implicit any compiler errors).
- `[x]` Task 066: Implement Postgres Persistence & WhatsApp Synchronous Caching (Migrated ConfigPlatformService to standard asynchronous database persistence with idempotent migration scripts; designed an in-memory write-through cache in runtime-config.ts to support high-performance synchronous chatbot key lookups; resolved strict exactOptionalPropertyTypes compilation errors across all admin endpoints using conditional object spreads; and successfully updated the translation request and webhook integration test suites to compile and execute cleanly with 100% test coverage passing).
- `[x]` Task 068: Resolve Hydration Mismatch & Previews TypeScript Resolution Error (Resolved the React hydration mismatch console error on `/login` caused by SSR dynamic translation text difference and browser-extension-injected attributes by introducing a unified `LoginPageFallback` component, implementing a client-side `mounted` state guard to delay interactive renders until hydration is complete, and adding `suppressHydrationWarning` to key layout nodes. Also resolved a TypeScript compile issue in `IntegrationWorkspacePreview.tsx` by removing the obsolete import and usage of the deleted `AdminAccessKeyPanel` component, ensuring a successful production build)
- `[x]` Task 069: Login Form Forgot Password Link, Help Placement & WCAG Contrast Fix (Added dedicated "Forgot password?" link between password field and Sign in button with brand-colored styling; moved "Get sign-in help" into a visually separated footer zone below a horizontal divider to prevent confusion with form actions; fixed WCAG AA contrast on dark aside panel highlight labels by replacing `rgba(226,232,240,0.72)` with solid `#cbd5e1` for 7.4:1 ratio; updated component preview workshop to match)
- `[x]` Task 070: Login Page Layout Fix — Overflowing Left Panel Content (Moved the support block and footer footnote from the cramped left panel to the bottom of the right aside panel; reduced left panel grid rows from 4 to 2; switched aside panel from grid to flex column with `margin-top: auto` to pin support to the bottom; added dark-context colour overrides so moved elements render correctly on the dark navy aside background)
- `[x]` Task 071: Trim Redundant Login Hero & Form-Card Header (Removed the in-card "Secure sign-in / Admin sign in / Enter your assigned credentials..." header since the same intent is conveyed by the left-panel hero; removed the "Executive admin access" badge and the three-metric `Role-aware / 3 core / Session-backed` hero strip from the login left panel; dropped the now-unused `eyebrow`, `title`, `description`, `heroBadge`-on-login, and `heroHighlights` props from the `LoginFormCard` and the login `AuthPageShell` callsites, removed `heroHighlights` from the shell's prop type entirely, dropped the orphan `Badge` import in two files, and deleted the corresponding CSS rules — `auth-login-card__header/__eyebrow/__title/__description` and `auth-shell__hero-highlights/__hero-strip/__hero-metric*` — plus their compact-density and responsive overrides; preview workshop updated to match)
- `[x]` Task 072: Aside Label Dark-Background Contrast Fix (`auth-shell__aside-label` was rendering `var(--color-brand-700)` `#253941` on the dark navy aside panel, producing dark-on-dark text that was effectively invisible; switched the rule to `var(--color-brand-300)` `#9fb4bc`, giving ~8:1 contrast (WCAG AAA) while keeping the eyebrow brand-tinted and tokenised — no raw hex introduced)
- `[x]` Task 073: Local Dev Resilience — `.env.local` Loading & Non-Fatal Startup Migrations (Updated the backend `dev` script to pass Node's native `--env-file-if-exists=../.env.local` flag through tsx so local secrets — including `ADMIN_AUTH_BOOTSTRAP_EMAIL/PASSWORD` and `POSTGRES_URL` — actually load on `npm run dev`; the `-if-exists` variant is silent in CI/prod where the file is absent, matching the local-only env override rule. Wrapped `runMigrations()` and `ensureCacheInitialized()` in the backend boot path with `try/catch` + warning logs so an unreachable Postgres (e.g. firewall blocking the remote staging DB from a developer's machine) no longer kills the process — admin auth is in-memory and works without Postgres; the in-memory config fallback fills in for the runtime cache. The CLI-direct `npm run migrate` path still throws loudly so production deploys catch real Postgres failures)
